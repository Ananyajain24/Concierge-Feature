"use client";

// Placeholder; filters wire up when the queue has multiple destinations.
export function ReviewFilters() {
  return (
    <div className="text-xs text-muted">Sorted by SLA · closest breach first</div>
  );
}
